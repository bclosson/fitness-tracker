<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Tracker</title>
</head>
<body>
    <%- include('../partials/header') %>

    <main>
        <h2>All Workouts</h2>
        <ul>
          <% for(let i = 0; i < workout.length; i++){ %>
              <li>
                <a href="/workouts/<%= workouts[i]._id %>"><%= workouts[i].name %></a>
              </li>
          <% } %>
        </ul>
      </main>
</body>
</html>