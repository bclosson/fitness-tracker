<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Tracker</title>
</head>
<body>
    <%- include('../partials/header') %>
    
  <main>
    <h2>All Workouts</h2>
    <ul>
      <% allWorkouts.forEach((workout, index) => { %>
        <li>
          <a href="/workouts/<%= workout.id %>">
            <h2><%= index + 1 %>. <%= workout.name %></h2>
          </a>
          <p><%= workout.date %></p>
        </li>
      <% }) %>
      </ul>
    </main>
  </body>
  </html>